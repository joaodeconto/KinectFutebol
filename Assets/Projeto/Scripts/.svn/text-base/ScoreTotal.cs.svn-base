using UnityEngine;
using System.Collections;

[AddComponentMenu("VisioramaKinect/ScoreTotal")]
public class ScoreTotal : MonoBehaviour
{
	internal int scoreTotal = 0;
	
	void OnGUI()
	{
		GUI.Box(new Rect(Screen.width / 2 - 50, 10, 100, 20), "SCORE: "+scoreTotal);
	}
}

